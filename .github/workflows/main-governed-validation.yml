name: EasyCommunity Main Governed Validation

on:
  push:
    branches:
      - agent/easycommunity-main-governed-validation-2026-08-12
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Compile backend
        run: python -m compileall -q app.py

      - name: Run complete current-main plus governed UAT regression suite
        run: python -m pytest -q

      - name: Check all JavaScript syntax
        shell: bash
        run: |
          find static -name '*.js' -print0 | while IFS= read -r -d '' file; do
            node --check "$file"
          done
